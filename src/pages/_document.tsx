import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
    return (
        <Html lang="en">
            <Head />
            <body className="font-sans bg-lines min-h-screen h-full overflow-auto">
                <Main />
                <NextScript />
            </body>
        </Html>
    );
}
